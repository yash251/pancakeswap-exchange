{
  "name": "pancakeswap-exchange",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ pancakeswap-exchange",
    "create-local": "graph create --node http://localhost:8020/ pancakeswap-exchange",
    "remove-local": "graph remove --node http://localhost:8020/ pancakeswap-exchange",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pancakeswap-exchange",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.74.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
